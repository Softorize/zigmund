name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: goto-bus-stop/setup-zig@v2
        with:
          version: 0.15.2
      - name: Run tests
        run: zig build test
      - name: Run perf benchmark families (progressive baseline)
        run: zig build perf
      - name: Generate parity matrix
        run: zig build parity-report
      - name: Enforce parity gate (progressive)
        env:
          PARITY_REQUIRED_IMPLEMENTED: "1"
          PARITY_MAX_STUB: "115"
          PARITY_MAX_MISSING: "0"
        run: sh tools/parity/check_parity_gate.sh
      - name: Generate SBOM
        run: zig build run -- sbom --out zigmund.sbom.json
      - name: Governance checks
        run: sh tools/release/check_governance.sh
      - name: Build release artifacts
        run: sh tools/release/build_release_artifacts.sh
      - name: Upload SBOM artifact
        uses: actions/upload-artifact@v4
        with:
          name: zigmund-sbom
          path: zigmund.sbom.json
      - name: Upload release artifacts
        uses: actions/upload-artifact@v4
        with:
          name: zigmund-release
          path: dist/release/*
